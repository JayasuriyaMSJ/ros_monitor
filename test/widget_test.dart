// This is a placeholder widget test.
// The app uses RosMonitorApp, not MyApp (the boilerplate default).
// Real integration tests require a live rosbridge connection.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // TODO: add real tests
    expect(true, isTrue);
  });
}
